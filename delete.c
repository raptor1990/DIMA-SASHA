BOLSHIE SIIIISKIIII!!!!!!



#include <stdio.h>
#include <ctype.h>
#include <stdlib.h>

#define MAX 20
#define MIN_LIMIT 7

struct bintr {
    return (struct bintr *) malloc(sizeof(struct bintr));
}

char *strdupl(char *w)
{
    char *p = (char *) malloc(strlen(w));
    if (p != NULL) {
        strcpy(p, w);
    }
    return p;
}
